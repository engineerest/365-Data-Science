import numpy as np # NumPy - a third-party package allowing us to work with multidimensional arrays

import pandas as pd # Pandas - allows us to organize data in a tabular form
# and to attach descriptive labels to the rows and columns

import scipy

import statsmodels.api as sm # StatsModels - a package built on top of NumPy and SciPy, which integrates with pandas.
# SM provides very good summaries

import matplotlib.pyplot as plt # Matplotlib - a 2D plotting library specially designed for visualization of NumPy computations

import seaborn as sns # Seaborn - Seaborn is a Python visualization library based on matplotlib.
# It provides a high-level interface for drawing attractive statistical graphics

import sklearn # scikit-learn - sklearn is one of the most widely used machine learning libraries